default-group: local
groups:
  local:
    generators:
      - name: fernapi/fern-typescript-browser-sdk
        version: 0.9.5
        output:
          location: local-file-system
          path: ../src/api-client
        config:
          outputEsm: true
          outputSourceFiles: true
